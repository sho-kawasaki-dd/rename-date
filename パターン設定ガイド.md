# rename-date パターン設定ガイド

## 1. このガイドについて

`rename-date` の「パターンプリセット」には、ファイル名の中から日付部分を見つけるための正規表現を登録します。

正規表現に慣れていない場合は、まず本ガイドの設定例をそのまま利用してください。ファイル名の形式が例と異なる場合は、後半の LLM への質問テンプレートを使ってパターンを作成できます。

パターン設定後は、必ずプレビューで変更前後のファイル名を確認してから実行してください。

## 2. 最初に知っておくルール

`rename-date` のパターンには、次の3つのキャプチャグループが必要です。

```text
(年)(月)(日)
```

左から順に、次の値を表します。

1. 年
2. 月
3. 日

たとえば、次のパターンは3つのグループを持っています。

```regex
\((\d{4})\.(\d{1,2})\.(\d{1,2})\)
```

このパターンは、次の文字列に一致します。

```text
(2024.1.5)
```

正規表現を登録するときは、画面上では次の文字列を入力します。

```text
\((\d{4})\.(\d{1,2})\.(\d{1,2})\)
```

## 3. そのまま使える設定例

以下の例では、出力テンプレートとして既定の `{Y}{M}{D}` を選ぶと、日付部分が `20240105` の形式に変換されます。

### 3.1 `(2024.1.5)` 形式

ファイル名の例：

```text
写真 (2024.1.5).jpg
```

パターン名の例：

```text
括弧付きドット区切り
```

正規表現：

```text
\((\d{4})\.(\d{1,2})\.(\d{1,2})\)
```

変換結果の例：

```text
写真 20240105.jpg
```

### 3.2 `2024-1-5` 形式

ファイル名の例：

```text
請求書_2024-1-5_田中.pdf
```

パターン名の例：

```text
ハイフン区切り
```

正規表現：

```text
(\d{4})-(\d{1,2})-(\d{1,2})
```

変換結果の例：

```text
請求書_20240105_田中.pdf
```

### 3.3 `2024/1/5` 形式

ファイル名の例：

```text
作業記録_2024/1/5.txt
```

Windows のファイル名では `/` は通常使えないため、この例はファイル名そのものには利用できません。フォルダ名や別の文字列形式と混同しないよう注意してください。

### 3.4 `2024年1月5日` 形式

ファイル名の例：

```text
会議資料_2024年1月5日.docx
```

パターン名の例：

```text
日本語年月日
```

正規表現：

```text
(\d{4})年(\d{1,2})月(\d{1,2})日
```

変換結果の例：

```text
会議資料_20240105.docx
```

### 3.5 `20240105` 形式

ファイル名の例：

```text
バックアップ_20240105.zip
```

パターン名の例：

```text
数字8桁
```

正規表現：

```text
(\d{4})(\d{2})(\d{2})
```

変換結果の例：

```text
バックアップ_20240105.zip
```

変換前後の名前が同じ場合、そのファイルは変更対象から除外されます。

### 3.6 `2024_01_05` 形式

ファイル名の例：

```text
報告書_2024_01_05.xlsx
```

パターン名の例：

```text
アンダースコア区切り
```

正規表現：

```text
(\d{4})_(\d{1,2})_(\d{1,2})
```

変換結果の例：

```text
報告書_20240105.xlsx
```

### 3.7 `2024.01.05` 形式

ファイル名の例：

```text
日報_2024.01.05.txt
```

パターン名の例：

```text
ドット区切り
```

正規表現：

```text
(\d{4})\.(\d{1,2})\.(\d{1,2})
```

変換結果の例：

```text
日報_20240105.txt
```

## 4. 正規表現の最小限の説明

### `\d`

数字1文字を表します。

```text
\d
```

例：`0`、`5`、`9`

### `{4}` と `{1,2}`

直前の指定が何文字続くかを表します。

```text
\d{4}
```

数字4桁。主に年に使います。

```text
\d{1,2}
```

数字1桁または2桁。月や日に使います。

### `( ... )`

値を取り出すグループです。`rename-date` では、年・月・日の3グループが必要です。

```text
(\d{4})(\d{1,2})(\d{1,2})
```

### `\.`

ピリオドそのものを表します。正規表現ではピリオドに特別な意味があるため、文字として扱うときは前に `\` を付けます。

```text
\.
```

### `\(` と `\)`

括弧そのものを表します。日付を囲む括弧を検索したい場合に使います。

```text
\( ... \)
```

### 文字をそのまま書く

ハイフン、アンダースコア、日本語の「年」「月」「日」などは、多くの場合そのまま書けます。

```text
(\d{4})年(\d{1,2})月(\d{1,2})日
```

## 5. よくある間違い

### キャプチャグループが3つではない

次のパターンはグループが1つしかないため、登録できません。

```text
\(\d{4}\.\d{1,2}\.\d{1,2}\)
```

年・月・日をそれぞれ括弧で囲みます。

```text
\((\d{4})\.(\d{1,2})\.(\d{1,2})\)
```

### ピリオドを `.` と書く

`.` は任意の1文字に一致する特別な記号です。ピリオドそのものを指定する場合は `\.` と書きます。

```text
誤り:  (\d{4}).(\d{1,2}).(\d{1,2})
正解:  (\d{4})\.(\d{1,2})\.(\d{1,2})
```

### 月日を必ず2桁にしてしまう

`2024.1.5` と `2024.01.05` の両方に一致させたい場合は、月日を次のようにします。

```text
\d{1,2}
```

2桁だけに限定する場合は、次のようにします。

```text
\d{2}
```

### 年月日の順番を間違える

次のパターンは、左から年・月・日の順である必要があります。

```text
(年)(月)(日)
```

日・月・年の順で保存されたファイル名には、その順番に合わせたパターンが必要です。ただし、出力時の `{Y}`、`{M}`、`{D}` は、キャプチャした年・月・日に対応します。

### 余計な文字まで一致させる

ファイル名の共通部分を広く指定しすぎると、意図しない箇所が変換対象になることがあります。最初は日付部分だけを指定し、プレビューで結果を確認してください。

## 6. 複数パターンを使う場合

パターンは複数選択できます。選択したパターンは、一覧に表示されている順番で適用されます。

たとえば、同じフォルダに次の2種類のファイルがある場合を考えます。

```text
報告_2024-1-5.txt
写真 (2024.1.5).jpg
```

次の2つのパターンを登録して選択します。

```text
(\d{4})-(\d{1,2})-(\d{1,2})
\((\d{4})\.(\d{1,2})\.(\d{1,2})\)
```

どちらのパターンも、選択した出力テンプレートに従って変換されます。

パターンの順番によって結果が変わる場合があるため、複数選択時はプレビューで確認してください。

## 7. LLM に正規表現を作ってもらう方法

ChatGPT、GitHub Copilot などの LLM に質問するときは、実際のファイル名をできるだけ複数伝えてください。

特に次の情報を含めると、回答の精度が上がります。

- ファイル名の実例を2〜5個
- 日付部分がどこにあるか
- 年・月・日の順番
- 月日が1桁になるか2桁になるか
- 日付の前後にある記号
- 変換後にしたい形式
- `rename-date` の3キャプチャグループ制約

### そのまま使える質問テンプレート

```text
ファイル名に含まれる日付を、rename-date で変換したいです。

ファイル名の例:
- 請求書_2024-1-5_田中.pdf
- 請求書_2024-12-31_佐藤.pdf
- 請求書_2025-3-7_鈴木.pdf

日付部分は YYYY-M-D 形式です。
年・月・日の順序は変わりません。
月と日は1桁または2桁です。

rename-date の条件に合う正規表現を作ってください。
条件:
- キャプチャグループは年・月・日の3つだけ
- 1つ目のグループは年、2つ目は月、3つ目は日
- ファイル名の中の日付部分だけに一致させる

次の形式で回答してください。
1. パターン名
2. rename-date に入力する正規表現
3. 3つのキャプチャグループの意味
4. 例のファイル名に適用した変換結果
5. 一致しないファイル名の例
```

### 特殊な形式を質問する例

```text
次のファイル名に含まれる日付部分に一致する、rename-date用の正規表現を作ってください。

例:
- 会議資料_2024年1月5日.docx
- 会議資料_2024年12月31日.docx

条件:
- キャプチャグループは年・月・日の3つだけ
- 月と日は1桁または2桁
- 日付以外のファイル名部分には一致させない
- rename-dateの出力テンプレート {Y}{M}{D} で 20240105 に変換できること

正規表現、各グループの意味、変換結果、注意点を説明してください。
```

### LLM に修正を依頼する例

正規表現がうまく動かない場合は、実際のプレビュー結果と期待する結果を伝えます。

```text
提案された正規表現を rename-date に登録しましたが、期待した結果になりません。

登録したパターン:
(ここにパターンを貼り付ける)

実際のファイル名:
(ここにファイル名を貼り付ける)

実際の変換結果:
(プレビューに表示された結果)

期待する変換結果:
(期待するファイル名)

rename-date の条件に合わせて、原因と修正版の正規表現を示してください。
キャプチャグループが年・月・日の3つになっているかも確認してください。
```

## 8. LLM の回答を使うときの注意

LLM が作った正規表現を、そのまま実行に使わないでください。次の手順で確認します。

1. 回答から正規表現だけをコピーする
2. パターンプリセットに登録する
3. 実際のファイルを少数だけ対象にする
4. プレビュー更新を押す
5. 変更前後のファイル名が期待どおりか確認する
6. 問題がなければ一括変換を実行する

次の点は必ず確認してください。

- キャプチャグループが年・月・日の3つである
- 3つのグループの順番が年・月・日になっている
- 日付以外の文字を誤って一致させていない
- 1桁と2桁の日付を想定どおり扱える
- 不正な日付が変換対象になっていない
- 拡張子が変わっていない

ファイル名には個人情報や機密情報が含まれることがあります。外部の LLM に質問する場合は、必要に応じて会社名、顧客名、氏名、案件名などを伏せ字や架空の文字列に置き換えてください。

## 9. うまく一致しないときの確認表

| 確認項目 | 例 |
| --- | --- |
| 年月日の順番 | `(年)(月)(日)` になっているか |
| グループ数 | `(...)` が年・月・日の3つあるか |
| 区切り文字 | `.`、`-`、`_`、`年` などが実際の名前と一致しているか |
| ピリオド | 文字としての `.` は `\.` と書いているか |
| 月日の桁数 | 1桁または2桁なら `\d{1,2}` か |
| 実在する日付 | 月13、4月31日などになっていないか |
| 対象範囲 | 日付以外を広く一致させていないか |

## 10. 関連資料

- [rename-date 取扱説明書](取扱説明書.md): アプリ全体の操作方法
