# rename-date 取扱説明書

## 1. 概要

`rename-date` は、ファイル名に含まれる日付表現を正規表現で検索し、指定した形式へ一括変換する Windows 向けアプリです。

主な機能は次のとおりです。

- フォルダ内のファイルを再帰的に検索
- ファイルやフォルダのドラッグ＆ドロップ
- 変更前後のプレビュー
- 正規表現パターンの複数選択
- 出力形式のプリセット管理
- ファイル名の衝突回避
- 直前の変換操作の Undo
- 処理ログの保存
- 保存した処理ログの画面表示

## 2. 起動

配布版をインストールした場合は、スタートメニューまたはデスクトップのショートカットから起動します。

開発環境から起動する場合は、次のコマンドを実行します。

```powershell
uv sync
uv run main.py
```

同じアプリを複数起動した場合、新しい画面は起動せず、すでに起動している画面が前面に表示されます。

## 3. 基本操作

### 3.1 対象を選択する

設定エリアで、次のいずれかの方法により対象を追加します。

- フォルダ選択でフォルダを選ぶ
- フォルダをドラッグ＆ドロップする
- 個別ファイルをドラッグ＆ドロップする

フォルダを指定すると、サブフォルダ内も検索されます。複数の対象を追加することもできます。同じ対象を複数回追加した場合は、重複して処理されません。

不要な対象は一覧から選択し、選択項目を削除します。

### 3.2 パターンを選択する

パターンプリセットから、ファイル名内の日付表現に対応するパターンを選択します。

複数のパターンを選択した場合は、一覧の上から順番に適用されます。各パターンには、年・月・日の3つのキャプチャグループが必要です。

既定のパターンは、次の形式に対応します。

```text
(2024.1.5)
```

### 3.3 出力形式を選択する

出力テンプレートから、変換後の日付形式を1つ選択します。

既定のテンプレートは次のとおりです。

```text
{Y}{M}{D}
```

プレースホルダーの意味は次のとおりです。

- `{Y}`: 4桁の年
- `{M}`: 2桁の月
- `{D}`: 2桁の日

変換例：

```text
(2024.1.5) → 20240105
```

### 3.4 プレビューを確認する

対象、パターン、出力テンプレートを選択して「プレビュー更新」を押すと、変更予定の一覧が表示されます。

一覧には次の情報が表示されます。

- 状態
- 変更前ファイル名
- 変更後ファイル名
- ファイルのパス

内容を確認し、問題がなければ「一括変換を実行」を押します。

プレビューは表示専用です。個別のファイルだけを選択して実行することはできません。

## 4. プレビューの状態

### 実行待ち

通常どおり変換されるファイルです。

### 衝突回避

変換後の名前が既存ファイルや同じ処理内の別ファイルと重複する場合、自動的に `_1`、`_2` などの番号が付加されます。

```text
report 20240105.txt
report 20240105_1.txt
report 20240105_2.txt
```

既存ファイルを上書きすることはありません。

### 無効な日付

日付として成立しない値は変換されません。

```text
(2024.13.45)
```

この項目はプレビューに表示されますが、実行対象から除外されます。

### 表示されないファイル

次のファイルはプレビューに表示されません。

- パターンに一致しないファイル
- 変換前後で名前が変わらないファイル
- フォルダ走査時の隠しファイル・隠しフォルダ
- `.git`、`.svn`、`.hg`
- `node_modules`
- `__pycache__`
- `.venv`、`venv`
- `.idea`、`.vscode`
- シンボリックリンクやジャンクション

走査対象はファイルのみです。フォルダ名は変更されません。

## 5. 実行・キャンセル・Undo

処理中は進捗バーとステータス欄に進行状況が表示されます。

処理を止めたい場合は「キャンセル」を押します。キャンセル時点までに完了した処理が残る場合があります。

変換後は「Undo」を押すと、直前の一括変換を取り消せます。

- 複数回の変換は新しいものから順に取り消されます
- 復元対象のファイルが削除済みの場合、そのファイルだけスキップされます
- 復元先に別のファイルが存在する場合、そのファイルだけスキップされます
- Undo 履歴はアプリ終了時に失われます
- Undo 履歴は保存されないため、再起動後に元へ戻すことはできません

## 6. プリセットの管理

パターンプリセットと出力テンプレートは、それぞれ「新規」「編集」「削除」で管理できます。

### パターンプリセット

パターンは、年・月・日の3つのキャプチャグループを持つ正規表現で指定します。正規表現として無効な場合や、キャプチャグループが3つでない場合は保存できません。

### 出力テンプレート

出力テンプレートには `{Y}`、`{M}`、`{D}` をすべて1回以上含める必要があります。Windows のファイル名に使用できない文字は指定できません。

### 共通の注意事項

- 編集時はプリセット名を変更できません
- 名前を変更したい場合は、新しいプリセットを作成してから古いプリセットを削除します
- 同じ名前で保存すると、既存のプリセットが上書きされます
- 最後の1件は削除できません
- プリセットはアプリ終了後も保存されます

## 7. ログ

「ログ保存」が有効な場合、変換と Undo の結果がログに記録されます。変換時のファイルごとの成功、スキップ、エラーなどを確認できます。

画面上部の「ログ」タブを選択すると、保存されたログを一覧で確認できます。ログは新しいものから順に表示されます。「更新」を押すと現在のログファイルを読み直します。「ログフォルダを開く」を押すと、ログの保存先をエクスプローラーで開きます。

ログの保存先は次のとおりです。

```text
%APPDATA%\rename-date\logs\rename_log.txt
```

ログファイルは一定のサイズを超えると世代管理されます。

## 8. 保存される設定

パターンプリセットの保存先：

```text
%APPDATA%\rename-date\config\patterns.json
```

出力テンプレートの保存先：

```text
%APPDATA%\rename-date\config\output_templates.json
```

設定ファイルが存在しない、壊れている、または形式が正しくない場合は、既定のプリセットで再作成されます。

## 9. 注意事項

- 拡張子は変更されません
- 変換対象はファイル名の本体部分だけです
- プレビューに表示された項目は個別に選択できず、一括で実行されます
- ファイルが他のアプリで使用中の場合、変換に失敗することがあります
- 実行中にアプリを終了すると、処理途中の変更が残る場合があります
- 実行中に終了した場合、ログや Undo 履歴が正しく記録されないことがあります
- 重要なファイルを処理する前に、必要に応じてバックアップを作成してください

## 10. 困ったときは

### 何も表示されない

対象、パターン、出力テンプレートが選択されているか確認してください。また、ファイル名が選択したパターンに一致しているか確認してください。

### 正規表現エラーが表示される

パターンが正しい正規表現か確認してください。年・月・日の3つのキャプチャグループが必要です。

### ファイルが変換されない

次の点を確認してください。

- 日付が実在する日付か
- ファイル名がパターンに一致しているか
- 変換前後でファイル名が同じになっていないか
- ファイルが他のアプリで開かれていないか

### 変換後の名前に `_1` が付く

同じフォルダ内に変換後と同じ名前のファイルが存在しています。上書きを避けるため、自動的に連番が付加されています。
