[project]
name = "rename-date"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sho_kawasaki", email = "tenjindonran@yahoo.co.jp" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
rename-date = "rename_date.__main__:main"

[dependency-groups]
dev = [
    "pyinstaller>=6.21.0",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]

[build-system]
requires = ["uv_build>=0.11.14,<0.12.0"]
build-backend = "uv_build"
